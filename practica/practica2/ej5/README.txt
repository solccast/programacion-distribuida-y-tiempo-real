# Uso 

> Es necesario tener Vagrant disponible 
- vagrant up : se levantan las dos VM con los archivos listos y compilados. 
- Una vez finalizado el setup: 
    -Terminal 1: se ejecutan `vagrant ssh vm1` y `./run_server.sh`  
    -Terminal 2: `vagrant ssh vm2` y luego `./run_client.sh`. En el que los resultados se volcarán en el archivo resultados_ej5.txt en el mismo directorio donde se encuentra el vagrantfile. 

  